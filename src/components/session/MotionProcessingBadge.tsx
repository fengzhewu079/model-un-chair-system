import React from 'react';

interface MotionProcessingBadgeProps {
  motionId: string;
}

export const MotionProcessingBadge: React.FC<MotionProcessingBadgeProps> = ({
  motionId: _motionId,
}) => {
  return null;
};
